# GitHub Copilot Instructions

The canonical repository guidance is in [`../AGENTS.md`](../AGENTS.md). Read and
follow that file before making changes. It documents the current Windows-first Jekyll
workflow, manifest-driven media pipeline, architecture contracts, and validation
commands.

Do not duplicate those instructions here; update `AGENTS.md` when the project workflow
changes so every coding agent receives the same guidance.
