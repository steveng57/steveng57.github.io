# The Site Configuration

# Import the theme
theme: jekyll-theme-chirpy
incremental: false

# Use shared includes from submodule
includes_dir: _shared/_includes
# The language of the webpage › http://www.lingoes.net/en/translator/langcode.htm
# If it has the same name as one of the files in folder `_data/locales`, the layout language will also be changed,
# otherwise, the layout language will use the default value of 'en'.
lang: en
encoding: UTF-8

# Change to your timezone › https://kevinnovak.github.io/Time-Zone-Picker
timezone: America/Toronto

# jekyll-seo-tag settings › https://github.com/jekyll/jekyll-seo-tag/blob/master/docs/usage.md
# ↓ --------------------------

title: Steve's Stuff

tagline: A collection of woodworking things and other stuff. # it will display as the sub-title

description: >- # used by seo meta and the atom feed
  A bunch of postings on woodworking and other things that interest me.

# Fill in the protocol & hostname for your site.
# e.g. 'https://username.github.io', note that it does not end with a '/'.
url: "https://www.stevengoulet.com" # the base hostname & protocol for your site

github:
  username: steveng57

#twitter:
#  username: twitter_username # change to your twitter username

social:
  # Change to your full name.
  # It will be displayed as the default author of the posts and the copyright owner in the Footer
  name: Steven Goulet
  email: steveng57@outlook.com # change to your email address
  links:
    # The first element serves as the copyright owner's link
    - https://www.facebook.com/steven.goulet.58
    - https://github.com/steveng57 # change to your github homepage
    - https://www.linkedin.com/in/steven-goulet-551451
    # Uncomment below to add more social links
    # - https://twitter.com/username # change to your twitter homepage

google_site_verification: 5IhkIIuL1MraUQQs3bMejAi6fZYn20p3r7oR9gPdlkc

# Environment-specific configuration
env:
  turnstile_site_key: '0x4AAAAAACDJGG8YoVocKgq2' # fallback for local dev

# ↑ --------------------------
# The end of `jekyll-seo-tag` settings

analytics:
  google:
    id: # fill in your Google Analytics ID
  goatcounter:
    id: '9830369'

# Pageviews settings
pageviews:
  provider: 'goatcounter'

goatcounter:
  id: '9830369'

# Prefer color scheme setting.
#
# Note: Keep empty will follow the system prefer color by default,
# and there will be a toggle to switch the theme between dark and light
# on the bottom left of the sidebar.
#
# Available options:
#
#     light  - Use the light color scheme
#     dark   - Use the dark color scheme
#
theme_mode: # [light|dark]

# The CDN endpoint for images.
# Notice that once it is assigned, the CDN url
# will be added to all image (site avatar & posts' images) paths starting with '/'
#
# e.g. 'https://cdn.com'
img_cdn:

# the avatar on sidebar, support local or CORS resources
avatar: /assets/img/favicons/android-chrome-192x192.png

# The URL of the site-wide social preview image used in SEO `og:image` meta tag.
# It can be overridden by a customized `page.image` in front matter.
social_preview_image: # string, local or CORS resources

# boolean type, the global switch for TOC in posts.
toc: true

comments:
  active: # The global switch for posts comments, e.g., 'disqus'.  Keep it empty means disable
  # The active options are as follows:
  disqus:
    shortname: # fill with the Disqus shortname. › https://help.disqus.com/en/articles/1717111-what-s-a-shortname
  # utterances settings › https://utteranc.es/
  utterances:
    repo: # <gh-username>/<repo>
    issue_term: # < url | pathname | title | ...>
  # Giscus options › https://giscus.app
  giscus:
    repo: # <gh-username>/<repo>
    repo_id:
    category:
    category_id:
    mapping: # optional, default to 'pathname'
    input_position: # optional, default to 'bottom'
    lang: # optional, default to the value of `site.lang`
    reactions_enabled: # optional, default to the value of `1`

# Self-hosted static assets, optional › https://github.com/cotes2020/chirpy-static-assets
assets:
  self_host:
    enabled: # boolean, keep empty means false
    # specify the Jekyll environment, empty means both
    # only works if `assets.self_host.enabled` is 'true'
    env: # [development|production]

pwa:
  enabled: true # the option for PWA feature

pagination:
  enabled: true
  trail:
    before: 2
    after: 2

# The base URL of your site
baseurl: ""

# ------------ The following options are not recommended to be modified ------------------

kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts: # Rouge Options › https://github.com/jneen/rouge#full-options
    css_class: highlight
    # default_lang: console
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: "" # An empty string here means all files in the project
      type: posts
    values:
      layout: post
      comments: true # Enable comments in posts.
      toc: true # Display TOC column in posts.
      # DO NOT modify the following parameter unless you are confident enough
      # to update the code of all other post links in this project.
      permalink: /posts/:title/
  - scope:
      path: _drafts
    values:
      comments: false
  - scope:
      path: ""
      type: tabs # see `site.collections`
    values:
      layout: page
      permalink: /:title/
  - scope:
      path: ""
      type: workshop
    values:
      layout: page
      comments: false
      permalink: /workshop/:slug/
  - scope:
      path: assets/img/favicons
    values:
      swcache: true
  - scope:
      path: assets/js/dist
    values:
      swcache: true

sass:
  style: compressed
  load_paths:
    - _shared/_sass

compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - LICENSE
  - rollup.config.js
  - package*.json
  - "*.ps1"
  - "*.cmd"
  - "*.afphoto"
  - tsconfig.json
  - "*.cmd"
  - deprecated/
  - assets/img/**/*.jpeg
  - assets/img/**/*.jpg

# Include additional directories
include:
  - _framework

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/

plugins:
  - jekyll-include-cache
  - jekyll-redirect-from
  - jekyll-paginate-v2

# Global Variables
tb: "{:target='_blank' rel='noopener noreferrer'}"